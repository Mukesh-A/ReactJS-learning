import React from 'react'

function FragmentDemo() {
  return (

    // when we use <div> tag and view thw website code in console we can seperately view the div 

    // when we use <> </> it doesnot show any div in the in console

    //just use <></> or <React.Fragment>
    // <React.Fragment>
    <>
    <h1>
      FragmentDemo
    </h1>
    <p>This is decsription</p>
    </>
    // </React.Fragment>
    
  )
}

export default FragmentDemo
